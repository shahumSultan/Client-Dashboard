import { Allura } from "next/font/google";

/** Rendering a typed name as a signature - only where a signature appears. */
export const signatureFont = Allura({
  subsets: ["latin"],
  weight: "400",
  display: "swap",
});
